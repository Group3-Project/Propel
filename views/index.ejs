<!DOCTYPE html>
<html>
<head>
	<%include partials/header.ejs %> 
</head>
<body>
	<!-- nav bar --> 
	<%include partials/nav.ejs %> 

   <% if (typeof user == 'object' && user) { %>
     <%=user.name%>
  <% } %>
	<!-- main body -->
	<% for (var game in game_list) { %>
      <h3><%= game_list[game].name %></h3>
    <% } %>

	
</body>
<script src="https://cdn.socket.io/socket.io-1.4.5.js"></script>
<script>
	var socket = io();
</script>

</html>